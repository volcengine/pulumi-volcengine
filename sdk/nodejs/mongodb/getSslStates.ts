// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Use this data source to query detailed information of mongodb ssl states
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as volcengine from "@pulumi/volcengine";
 * import * as volcengine from "@volcengine/pulumi";
 *
 * const fooZones = volcengine.ecs.getZones({});
 * const fooVpc = new volcengine.vpc.Vpc("fooVpc", {
 *     vpcName: "acc-test-vpc",
 *     cidrBlock: "172.16.0.0/16",
 * });
 * const fooSubnet = new volcengine.vpc.Subnet("fooSubnet", {
 *     subnetName: "acc-test-subnet",
 *     cidrBlock: "172.16.0.0/24",
 *     zoneId: fooZones.then(fooZones => fooZones.zones?.[0]?.id),
 *     vpcId: fooVpc.id,
 * });
 * const fooInstance = new volcengine.mongodb.Instance("fooInstance", {
 *     dbEngineVersion: "MongoDB_4_0",
 *     instanceType: "ReplicaSet",
 *     superAccountPassword: "@acc-test-123",
 *     nodeSpec: "mongo.2c4g",
 *     mongosNodeSpec: "mongo.mongos.2c4g",
 *     instanceName: "acc-test-mongo-replica",
 *     chargeType: "PostPaid",
 *     projectName: "default",
 *     mongosNodeNumber: 2,
 *     shardNumber: 3,
 *     storageSpaceGb: 20,
 *     subnetId: fooSubnet.id,
 *     zoneId: fooZones.then(fooZones => fooZones.zones?.[0]?.id),
 *     tags: [{
 *         key: "k1",
 *         value: "v1",
 *     }],
 * });
 * const fooSslState = new volcengine.mongodb.SslState("fooSslState", {instanceId: fooInstance.id});
 * const fooSslStates = volcengine.mongodb.getSslStatesOutput({
 *     instanceId: fooInstance.id,
 * });
 * ```
 */
export function getSslStates(args: GetSslStatesArgs, opts?: pulumi.InvokeOptions): Promise<GetSslStatesResult> {

    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("volcengine:mongodb/getSslStates:getSslStates", {
        "instanceId": args.instanceId,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getSslStates.
 */
export interface GetSslStatesArgs {
    /**
     * The mongodb instance ID to query.
     */
    instanceId: string;
    /**
     * File name where to save data source results.
     */
    outputFile?: string;
}

/**
 * A collection of values returned by getSslStates.
 */
export interface GetSslStatesResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * The mongodb instance id.
     */
    readonly instanceId: string;
    readonly outputFile?: string;
    /**
     * The collection of mongodb ssl state query.
     */
    readonly sslStates: outputs.mongodb.GetSslStatesSslState[];
    /**
     * The total count of mongodb ssl state query.
     */
    readonly totalCount: number;
}
/**
 * Use this data source to query detailed information of mongodb ssl states
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as volcengine from "@pulumi/volcengine";
 * import * as volcengine from "@volcengine/pulumi";
 *
 * const fooZones = volcengine.ecs.getZones({});
 * const fooVpc = new volcengine.vpc.Vpc("fooVpc", {
 *     vpcName: "acc-test-vpc",
 *     cidrBlock: "172.16.0.0/16",
 * });
 * const fooSubnet = new volcengine.vpc.Subnet("fooSubnet", {
 *     subnetName: "acc-test-subnet",
 *     cidrBlock: "172.16.0.0/24",
 *     zoneId: fooZones.then(fooZones => fooZones.zones?.[0]?.id),
 *     vpcId: fooVpc.id,
 * });
 * const fooInstance = new volcengine.mongodb.Instance("fooInstance", {
 *     dbEngineVersion: "MongoDB_4_0",
 *     instanceType: "ReplicaSet",
 *     superAccountPassword: "@acc-test-123",
 *     nodeSpec: "mongo.2c4g",
 *     mongosNodeSpec: "mongo.mongos.2c4g",
 *     instanceName: "acc-test-mongo-replica",
 *     chargeType: "PostPaid",
 *     projectName: "default",
 *     mongosNodeNumber: 2,
 *     shardNumber: 3,
 *     storageSpaceGb: 20,
 *     subnetId: fooSubnet.id,
 *     zoneId: fooZones.then(fooZones => fooZones.zones?.[0]?.id),
 *     tags: [{
 *         key: "k1",
 *         value: "v1",
 *     }],
 * });
 * const fooSslState = new volcengine.mongodb.SslState("fooSslState", {instanceId: fooInstance.id});
 * const fooSslStates = volcengine.mongodb.getSslStatesOutput({
 *     instanceId: fooInstance.id,
 * });
 * ```
 */
export function getSslStatesOutput(args: GetSslStatesOutputArgs, opts?: pulumi.InvokeOptions): pulumi.Output<GetSslStatesResult> {
    return pulumi.output(args).apply((a: any) => getSslStates(a, opts))
}

/**
 * A collection of arguments for invoking getSslStates.
 */
export interface GetSslStatesOutputArgs {
    /**
     * The mongodb instance ID to query.
     */
    instanceId: pulumi.Input<string>;
    /**
     * File name where to save data source results.
     */
    outputFile?: pulumi.Input<string>;
}
