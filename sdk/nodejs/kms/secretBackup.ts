// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to manage kms secret backup
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as volcengine from "@volcengine/pulumi";
 *
 * const _default = new volcengine.kms.SecretBackup("default", {secretName: "Test-1"});
 * ```
 *
 * ## Import
 *
 * KmsSecretBackup can be imported using the secret_name, e.g.
 *
 * ```sh
 * $ pulumi import volcengine:kms/secretBackup:SecretBackup default ecs-secret-test
 * ```
 */
export class SecretBackup extends pulumi.CustomResource {
    /**
     * Get an existing SecretBackup resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecretBackupState, opts?: pulumi.CustomResourceOptions): SecretBackup {
        return new SecretBackup(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'volcengine:kms/secretBackup:SecretBackup';

    /**
     * Returns true if the given object is an instance of SecretBackup.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecretBackup {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecretBackup.__pulumiType;
    }

    /**
     * The full backup data of the secret. JSON format.
     */
    public /*out*/ readonly backupData!: pulumi.Output<string>;
    /**
     * The ciphertext of the data key used to encrypt the secret value, Base64 encoded.
     */
    public /*out*/ readonly secretDataKey!: pulumi.Output<string>;
    /**
     * The name of the secret to backup.
     */
    public readonly secretName!: pulumi.Output<string>;
    /**
     * The signature of the backup_data. Base64 encoded.
     */
    public /*out*/ readonly signature!: pulumi.Output<string>;

    /**
     * Create a SecretBackup resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SecretBackupArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecretBackupArgs | SecretBackupState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecretBackupState | undefined;
            resourceInputs["backupData"] = state ? state.backupData : undefined;
            resourceInputs["secretDataKey"] = state ? state.secretDataKey : undefined;
            resourceInputs["secretName"] = state ? state.secretName : undefined;
            resourceInputs["signature"] = state ? state.signature : undefined;
        } else {
            const args = argsOrState as SecretBackupArgs | undefined;
            if ((!args || args.secretName === undefined) && !opts.urn) {
                throw new Error("Missing required property 'secretName'");
            }
            resourceInputs["secretName"] = args ? args.secretName : undefined;
            resourceInputs["backupData"] = undefined /*out*/;
            resourceInputs["secretDataKey"] = undefined /*out*/;
            resourceInputs["signature"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SecretBackup.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecretBackup resources.
 */
export interface SecretBackupState {
    /**
     * The full backup data of the secret. JSON format.
     */
    backupData?: pulumi.Input<string>;
    /**
     * The ciphertext of the data key used to encrypt the secret value, Base64 encoded.
     */
    secretDataKey?: pulumi.Input<string>;
    /**
     * The name of the secret to backup.
     */
    secretName?: pulumi.Input<string>;
    /**
     * The signature of the backup_data. Base64 encoded.
     */
    signature?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a SecretBackup resource.
 */
export interface SecretBackupArgs {
    /**
     * The name of the secret to backup.
     */
    secretName: pulumi.Input<string>;
}
